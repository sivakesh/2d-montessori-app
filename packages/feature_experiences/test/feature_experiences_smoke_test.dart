import 'package:feature_experiences/feature_experiences.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('feature_experiences package resolves and its barrel compiles', () {
    // Phase 0 placeholder: proves the workspace dependency graph
    // (core_contracts / design_system / firebase_adapters) resolves for
    // this feature. Replace with real domain/use-case tests as feature_experiences
    // grows real exports.
    expect(featureModuleName, 'feature_experiences');
  });
}
