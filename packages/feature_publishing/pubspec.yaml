# Publishing & Versions
# Editorial workflow states, immutable version snapshots, scheduling and rollback.
# Traceability: SRS CMS-02, CMS-05, CMS-06; PRD Section 9 (Editorial Workflow and Versioning)
name: feature_publishing
description: Publishing & Versions feature module for the 2D Montessori CMS/public site.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.15.0
  core_contracts:
    path: ../core_contracts
  design_system:
    path: ../design_system
  firebase_adapters:
    path: ../firebase_adapters

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
