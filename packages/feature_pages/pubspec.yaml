# Pages & Sections
# Managed pages, approved section-template registry, ordered sections and previews.
# Traceability: SRS WEB-01..WEB-03, CMS-01..CMS-15; PRD Section 4 (CMS Page Builder)
name: feature_pages
description: Pages & Sections feature module for the 2D Montessori CMS/public site.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.15.0
  core_contracts:
    path: ../core_contracts
  design_system:
    path: ../design_system
  firebase_adapters:
    path: ../firebase_adapters
  feature_publishing:
    path: ../feature_publishing

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
