import 'package:feature_settings/feature_settings.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('feature_settings package resolves and its barrel compiles', () {
    // Phase 0 placeholder: proves the workspace dependency graph
    // (core_contracts / design_system / firebase_adapters) resolves for
    // this feature. Replace with real domain/use-case tests as feature_settings
    // grows real exports.
    expect(featureModuleName, 'feature_settings');
  });
}
